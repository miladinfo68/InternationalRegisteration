@using ISR.Resources
@{
    ViewBag.Title = Resources.PasswordRecovery;
    Layout = "~/Views/Shared/_Layout.cshtml";
    //if (Session["Culture"] != null)
    //{
    //    System.Threading.Thread.CurrentThread.CurrentCulture = new System.Globalization.CultureInfo(Session["Culture"].ToString());
    //    System.Threading.Thread.CurrentThread.CurrentUICulture = new System.Globalization.CultureInfo(Session["Culture"].ToString());
    //}
}

<div id="contentHeight" class="col-md-12 contentBox">
    <div class="row fullHeight justify-content-center align-items-center">
        <div class="col-md-4 passwordRecoveryBox p-4">
            <div class="passwordResetMessage alert"></div>
            <input type="password" id="pwdNewPassword" class="form-control mb-2" placeholder="@Resources.PasswordReset_NewPassword" />
            <input type="password" id="pwdNewPasswordRepeat" class="form-control mb-2" placeholder="@Resources.PasswordReset_NewPasswordRepeat" />
            <a href="/" id="btnResetPassword" class="btn btn-success form-control" data-lang="@Session["lang"].ToString()">@Resources.PasswordReset_ChangePassword</a>
        </div>
    </div>
</div>
